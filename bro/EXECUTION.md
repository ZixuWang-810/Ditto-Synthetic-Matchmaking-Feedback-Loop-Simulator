# Execution Plan — Issue #5

**Objective**: I found two bugs:
    1. I got this warning after you implement embedding scoring.
18:46:54 | WARNING | src.ditto_bot.ma

---

## Task 19
Sync all runtime dependencies from `requirements.txt` into `pyproject.toml` so the `uv`-managed environment has every…

## Task 20
Update `README.md` at the project root to document the dependency sync fix. Add a short section (e.g., `## v0.1.1 —…

